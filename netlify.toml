[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Pretty endpoint: the page calls /api/report, which routes to the function.
[[redirects]]
  from = "/api/report"
  to = "/.netlify/functions/report"
  status = 200
